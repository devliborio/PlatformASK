<%-include('partials/header.ejs')%>
<body>
    <%-include('partials/navbar.ejs')%>

    <div class="container">
        <br>
        <p class="h1 d-flex justify-content-center font-weight-bold"> Perguntas </p>
        <br>
        <% questions.forEach((question) => {%>
            <br>
            <div class="card">

                <div class="card-body">
                    <h3><%=question.title%></h3>
                    <p><%=question.description%></p>
                </div>

                <div class="card-footer">
                    <a href="/question/<%=question.id%>"><button class="btn btn-primary">Responder</button></a>
                </div>
                
            </div>
        <% }) %>
        <br><br>
        <p class="text-right"><p>Pergunte aqui 👇</p> <a href="/ask" class="btn btn-primary">Faça uma pergunta</a></p>
    </div>

</body>
<%-include('partials/footer.ejs')%>